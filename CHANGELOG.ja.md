# 変更履歴

日付は公開日です。書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に沿っています。
英語版は [CHANGELOG.md](CHANGELOG.md) にあります。

## [0.5.0]

### 追加

- ツールメニューに**この端末に残っている未保存の内容**を追加した。
  同じ一覧を開けるので、控えを見るために再起動する必要がなくなった
- 控えに関する設定を追加した。控えを取らない、保持期間を選ぶ（1 / 7 / 30 / 90 日）、
  この端末の控えをすべて削除する、が選べる。控えを取らない設定では
  ファイル名の横の点がその旨を示し、保存先には何も書き込まない

### 修正

- ダイアログの説明と動作が食い違っていたのを修正。保持期間を過ぎた控えは
  提示した直後に削除されていたため、「あとで」を選んでも残らなかった。
  期限切れの削除を**一覧を出す前**に行うようにして説明どおりの動作にし、
  他のタブが使用中の控えは期間を過ぎていても削除しないようにした
- 破棄が即座に反映されていたのを修正。スマートフォンでは ✕ を誤って押しやすいため、
  ✕ と「すべて破棄」は行に印を付けるだけにした。ダイアログを閉じるまで
  実際には削除せず、もう一度押せば元に戻せる
- `-0` を安全と判定していたのを修正。JavaScript は `0` と区別できるが、
  書き出すと `0` になるため、往復できない他の数値と同じく変換を中止する

## [0.4.1]

### 修正

- 0.3 で残した下書きが 0.4 から見えなくなっていたのを修正。
  0.3 の下書きは共通のキーに保存され、レコード内にキーを持たなかったため、
  新しい一覧処理が除外していた。未保存の内容を持ったまま更新した場合、
  データは残っているのに復元できない状態だった。キーはレコードではなく
  保存庫側から読むようにした
- JSON ツールが内容を黙って変えることがあったのを修正。
  整形はブラウザのパーサを通るため、扱えない大きさの数値は丸められ、
  同じオブジェクトで重複したキーは最後のものだけが残る。
  該当する場合は変換を行わず、原因を示してカーソルをその位置へ移動するようにした。
  **JSON を検証**でも同じ内容を報告するので、問題になる前に気づける
- キーの並べ替えで `__proto__` が消えていたのを修正。
  JSON では普通のキーだが、通常のオブジェクトに代入するとプロパティではなく
  プロトタイプとして扱われ、出力から失われていた
- 他のタブが使用中の下書きが「前回の続き」として表示されていたのを修正。
  破棄すると、そのタブが次に入力するまでの間、唯一の控えが失われる状態だった。
  開いているタブが自分の下書きを申告し、その分を一覧から除くようにした
- 下書きの復元時に、新しいキーへの保存が成功したか確認せず旧キーを削除していたのを修正。
  保存だけ失敗すると両方失われる。保存が成功した場合にのみ旧キーを削除する
- 復元ダイアログが自身のボタン以外で閉じられると、セッションがキーを持たないまま
  になり、自動保存が働かなくなっていたのを修正

### 変更

- 残っている下書きを一覧で表示するようにした。ファイル名・最終更新時刻・内容の冒頭が並び、
  復元する・1 件だけ破棄する・すべて破棄する・あとにする、から選べる。
  1 回の起動につき 1 件ずつ尋ねる方式をやめた

## [0.4.0]

### 追加

- ステータスバーの上に、タブとソフトキーボードで奥にある記号
  （`{}`、`[]`、引用符、コロンなど）を入力する行を追加した。
  フォーカスを奪わないのでキーボードは開いたままで、
  タブはインデントの設定に従う。設定から非表示にもできる
- 行単位の編集を追加した（複製・削除・上へ移動・下へ移動）
- **JSON のキーを並べ替え**を追加した。キーを順に並べて整形するので、
  2 つの設定ファイルを行単位で比較できる
- ツールに**タブを挿入**を追加した（記号の行を非表示にしている場合のため）
- 選択中は、選択している量をステータスバーに表示するようにした
- 使い方の画面から GitHub のソースコードへのリンクを追加した

### 変更

- ファイル名の横の点が、未保存の内容が実際に端末へ控えられているかを示すようにした。
  控えられていないとき（内容が大きすぎる、保存先が使えない）はオレンジになる
- 下書きを 1 つの共通キーではなく、編集セッションごとに持つようにした。
  タブごと、共有から開いたファイルごとに別々の控えを持つ。
  残っている控えは 1 回の起動につき 1 件ずつ尋ね、
  30 日を過ぎても引き取られなかったものは削除する
- 自動保存で控えられる範囲と、その限界を使い方の画面と README に明記した

### 修正

- 共有メニューから開いたファイルが、以前の下書きを巻き込んで消すことがあったのを修正。
  編集すると下書きが上書きされ、保存すると削除されていた。いずれも、
  まだ尋ねていない内容が失われる。各セッションは自分のキーにだけ書くようにしたので、
  自分で書いたものしか消せない
- 保存の完了報告を、下書きの同期が終わるまで待つようにした。
  その一瞬に異常終了しても、保存前の状態が戻ってくることはなくなった

## [0.3.0]

### 変更

- リポジトリの記述言語を英語にした。README・変更履歴・コード内のコメント・テスト名を
  すべて英語で書き、日本語はドキュメントの第二言語として
  [README.ja.md](README.ja.md) と [CHANGELOG.ja.md](CHANGELOG.ja.md) で追従する
- ポルトガル語の表記から国名を外し、単に **Português** とした。
  辞書は `pt-br.js` から `pt.js` へ移し、どの地域のポルトガル語も同じ辞書を使う
- 行の並べ替えを、常に日本語の順序ではなく表示言語の順序に従うようにした
- ブラウザでの通し確認を英語で行い、日本語は第二言語として別に確認するようにした

### 修正

- 本文を全部消したときに下書きが復元されなかったのを修正。
  全文削除も編集の 1 つなので、空の状態のまま復元する
- プログラムからの読み込み（下書きの復元、文字コードを変えての開き直し）が
  その直後に下書きの削除を予約してしまい、未保存の内容が失われることがあったのを修正
- 保存に成功した時点で下書きを同期するようにした。
  直後に異常終了しても、保存前の状態が戻ってくることはなくなった
- 強調表示の上限（3,000 件）を超えた一致で「置換」が効かなかったのを修正。
  一覧の添字ではなく、選んでいる一致そのものをパネル側で保持するようにした

## [0.2.0]

### 追加

- 表示言語を 15 言語に対応（英語・フランス語・ドイツ語・イタリア語・スペイン語・
  ポルトガル語・日本語・簡体字中国語・繁体字中国語・韓国語・ヒンディー語・
  インドネシア語・ベトナム語・タイ語・アラビア語）。端末の言語から自動で選び、
  設定からいつでも変更できる。アラビア語では画面が右から左になる
- 編集中の内容を自動で控え、次の起動時に復元するか尋ねる（IndexedDB）
- 対応しているブラウザで「保存先を選ぶ」と「上書き保存」を追加
- 全文（選択中ならその範囲）をクリップボードへコピーするボタン
- 新しい版が入ったときに知らせ、その場で再読み込みできるようにした
- 使い方の画面に版を表示

### 変更

- 保存できない文字があるときは、書き出す前に止めて
  「UTF-8 で保存 / ? に置き換えて保存 / 取りやめ」を選べるようにした
- ファイル選択の拡張子の制限をなくし、代わりに読み込み時にバイナリらしさを判定して注意する
- 文字コードと改行コードの表示に枠と ▾ を付け、押して変えられることを示した
- 設定のアイコンを歯車に変更
- 入力欄の文字サイズを 16px 以上にし、iOS で画面が勝手に拡大しないようにした
- キーボード操作時に、選択中の場所が枠で分かるようにした

### 修正

- 保存した後の状態を揃えた。保存後は開き直しに保存内容が使われ、
  `?` に置き換えて保存した場合は編集中の内容との食い違いが未保存マークで分かる
- 書き込みに失敗したときは書きかけを確定させず、元のファイルを残すようにした
- 強調表示の上限（3,000 件）を超えた一致でも「次へ」で先に進むようにした

## [0.1.0]

### 追加

- 最初の版。テキストファイルの読み込み・編集・別名でのダウンロード保存
- 検索と置換（大文字小文字の区別・単語単位・正規表現）
- 文字コードの自動判別（UTF-8 / Shift_JIS / EUC-JP / ISO-2022-JP / UTF-16 / Windows-1252）と
  保存時の文字コード・改行コード・BOM の指定
- 元に戻す / やり直す、行番号、行へ移動、折り返しの切り替え
- JSON の整形・最小化・検証、行の並べ替えなどのツール
- PWA 化によるオフライン動作と、Android の共有メニューからの起動
